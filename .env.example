# Only the importer reads this private local setting. Copy to .env and set your directory.
STOCK_DATA_SOURCE=C:\path\to\stock-statistics\outputs
# Optional branding / subdirectory hosting settings (public values only).
VITE_SITE_NAME=A股涨跌统计
VITE_BASE_PATH=/
